# 分享說明（請先讀我）

這是「數位學伴」系統的**分享版**，已移除個人隱私與機密資料，可以安全地交給別人協助調整。

## 這份跟原版差在哪？（已移除的東西）

為了保護隱私，以下檔案**沒有**包含在這份分享版裡：

- **`.env`（兩個）** — 內含 LINE 金鑰、Access Token、你的 LINE User ID 等機密。改放對應的 `.env.example` 範本，接手的人自行複製填寫即可。
- **資料庫檔案** `backend/data/app.db`、`study-buddy-reminder/data/app.db`（含 `-shm`/`-wal`） — 內含實際任務、提醒與 LINE User ID 等個人資料。第一次啟動系統時會自動重新建立空的資料庫。
- **`.git/` 版本歷史** — 舊的 commit 曾把 `study-buddy-reminder` 的資料庫存進版本庫，為避免歷史外洩，整個 git 紀錄已移除。接手的人可自行 `git init` 重新開始。
- **`node_modules/`、`frontend/dist/`** — 相依套件與建置產物，執行 `npm install` / `npm run build` 就會重新產生。

## 保留下來的東西

- 全部原始碼（backend / frontend / study-buddy-reminder）
- `README.md`、`AGENTS.md` 等說明文件
- `.env.example` 環境變數範本（**不含真實金鑰**）
- `課表素材/` 內的課程表圖片（皆為網路上抓的公開課表截圖，作為上傳功能的測試範例，不含個人資訊）
- `backend/data/ai-settings.json`（api_key 為空白，無機密）

## 接手的人如何啟動

詳細步驟請看 `README.md`。快速版：

1. 安裝本機 LLM（Ollama 或 LM Studio）
2. 後端：`cd backend` → `npm install` → `cp .env.example .env`（再依需要填寫）
3. 前端：`cd frontend` → `npm install`
4. 啟動：後端 `npm run dev`、前端 `npm run dev`，開 http://localhost:5173

> ⚠️ 修改後端程式前，請先讀 `AGENTS.md`，並在改完後執行 `npm run check`（backend 內）確認回歸測試全綠。

## 給接手的人的提醒

系統目前有兩套實作：`backend`+`frontend`（TypeScript 主線）與 `study-buddy-reminder`（較早的 Node.js/EJS 版本）。以哪一套為準、要調整什麼，請向原作者確認。
